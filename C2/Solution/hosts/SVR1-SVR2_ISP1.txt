Преднастроенные узлы — НЕ ИЗМЕНЯТЬ
==================================

SVR1 / OPS-SRV:
  address 10.81.130.10/24, gateway 10.81.130.1
  DHCP scopes VLAN10,40,110,210
  DNS: ops/judge/internet-test records
  NTP, Syslog, SNMP manager, NetFlow UDP/2055
  HTTP /healthz -> OK-C2

SVR2 / JUDGE-SRV:
  address 10.81.150.10/24, gateway 10.81.150.1
  SSH console login: student / StudentPass

Проверка SNMPv3 с SVR1 и SVR2 (повторить для адресов всех Cisco):

  snmpwalk -v3 -l authPriv -u c2snmp -a SHA -A 'C2Auth@2026' \
    -x AES -X 'C2Priv@2026' -t 3 -r 0 10.255.80.1 1.3.6.1.2.1.1

Проверка management с SVR2:

  ssh admin@10.255.80.1
  # password Skill39@C2; show privilege -> 15
  nc -zvw3 10.255.80.1 23
  # TCP/23 должен быть CLOSED/timeout

ISP1 полностью преднастроен и менять его запрещено. На нем ожидаются:

  AS65000; customer peers .2/.6/.10/.14/.18; default route;
  Internet-test 198.51.100.100/32 и 8.8.8.8/32.

Проверка static NAT со стороны ISP1 при доступном IOS telnet-client:

  telnet 198.51.100.89 80
  GET /healthz HTTP/1.0
  Host: 198.51.100.89
  <пустая строка>

Ответ должен содержать ровно OK-C2 в body.
